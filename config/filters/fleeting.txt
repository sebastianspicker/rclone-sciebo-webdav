# Files that are safe to delete locally (`sciebo cleanup --junk --apply`).
# One shell glob per line, matched against file names; `#` comments and
# blank lines are ignored. Keep this list conservative: only files that are
# regenerated by the applications that create them belong here.
.DS_Store
._*
Thumbs.db
*.part
*.filepart
*.crdownload
